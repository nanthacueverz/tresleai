pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                // Run the unit tests
                // cargo test
                echo 'Unit tests'
                // connect to document db
            }
        }
    }
}
